services:
  - type: web
    name: lokagrow-demo
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app2.py
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHON_VERSION
        value: 3.11.0
